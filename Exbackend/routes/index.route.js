import express from "express";
import authRoutes from "./authroutes.js"; // Make sure the path is correct

const router = express.Router();

// router.use("/auth", authRoutes);

export default router;
